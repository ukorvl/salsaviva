/**
 * @file Viewport.
 */

import {Viewport} from 'next';

const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  colorScheme: 'dark',
  themeColor: '#18132d',
};

export default viewport;
